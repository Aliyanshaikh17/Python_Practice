#Find length of array without using len()
arr2 = [10,20,30]
count = 0
for i in arr2:
    count = count + 1
print(f"the length of arr2 is :- {count}")



#Find length of array with using len()

arr = [10,20,30]
print("Length of array:", len(arr))
